################################################################################
#                                                                              #
#                                HYPRLAND                                      #
#                                                                              #
################################################################################
# ─────────────────────────────────────────────────────────────────────────────
# Configuration Tips
#
# Hyprland allows a configuration file to be split into multiple smaller files.
# Each `source =` line below tells Hyprland to read and load another config file
# at startup, as if its contents were written directly in this file.
#
# This makes the config easier to organize, maintain, and version-control by
# grouping related settings (aesthetics, monitors, keybinds, rules, etc.) into
# separate files instead of one large monolithic config.
#
# If any sourced file is missing or contains an error, Hyprland will report it
# during startup or reload.
# ─────────────────────────────────────────────────────────────────────────────


source = $HOME/hyprdots/config/hypr/config/aesthetics.conf
source = $HOME/hyprdots/config/hypr/config/monitors.conf
source = $HOME/hyprdots/config/hypr/config//variables.conf
source = $HOME/hyprdots/config/hypr/config/autostart.conf
source = $HOME/hyprdots/config/hypr/config/environment.conf
source = $HOME/hyprdots/config/hypr/config/input-rules.conf
source = $HOME/hyprdots/config/hypr/config/permissions.conf
source = $HOME/hyprdots/config/hypr/config/keybindings.conf
source = $HOME/hyprdots/config/hypr/config/window-rules.conf
source = $HOME/hyprdots/config/hypr/config/assign-workspaces.conf
